# hemtt build output
.hemttout/
releases/

# built / binarized artifacts (regenerated by `hemtt release`)
*.pbo
*.bisign
*.biprivatekey

# signing public key (regenerated on build; ship via releases, not git)
keys/*.bikey

# build logs
build.out
build.err
release.out
release.err

# editor / OS noise
*.swp
*~
.DS_Store
Thumbs.db
